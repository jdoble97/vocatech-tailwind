import { GenerateModalDirective } from './generate-modal.directive';

describe('GenerateModalDirective', () => {
  it('should create an instance', () => {
    const directive = new GenerateModalDirective();
    expect(directive).toBeTruthy();
  });
});
